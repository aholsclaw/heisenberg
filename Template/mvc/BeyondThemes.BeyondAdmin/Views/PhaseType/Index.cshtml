@model IEnumerable<BeyondThemes.BeyondAdmin.Models.PhaseType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhaseTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhaseTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhaseTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PhaseTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhaseTypeID })
        </td>
    </tr>
}

</table>
