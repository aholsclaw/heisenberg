@model IEnumerable<BeyondThemes.BeyondAdmin.Models.TaskType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TaskTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TaskTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaskTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaskTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaskTypeID })
        </td>
    </tr>
}

</table>
